# 炫界云技术架构树

> 文档日期：2026-09-02
> 依据：当前 `power` 代码、现网部署、`gemini.md` 产品规划与《标准.md》开发标准。
> 定位：炫界云不是底层 GPU 厂商，而是统一账户、统一交易、统一产品、统一调度和多供应商接入的 AI 算力聚合平台。

## 1. 架构结论

SkyPilot确定为平台核心调度器。炫界云业务控制面负责账户、商品、订单、计费和业务状态，SkyPilot负责把自有/租赁GPU机器、Kubernetes集群和供应商资源统一为计算池，并调度平台自己的Docker镜像、Jobs和Services。

AutoDL弹性API与SkyPilot不是二选一：AutoDL Provider Adapter负责申请、查询和释放底层资源；SkyPilot负责资源选择、Docker运行环境、任务提交和服务生命周期。当前先接AutoDL，后续增加自有GPU池和其他供应商时继续复用同一套SkyPilot任务定义。

选型结论如下：

| 能力 | 当前选型 | 后续选型 | 结论 |
|---|---|---|---|
| Web 控制台 | React + TypeScript + Vite | 保持 | 已落地 |
| 平台业务 API | FastAPI | 按业务拆分 Router/Service，暂不拆微服务 | 已落地，需重构 |
| 交易与业务事实 | MySQL | 保持唯一事实源 | 已落地 |
| 实例与Docker调度 | SkyPilot API Server + 平台调度服务 | 接入更多云/Kubernetes/已有机器 | 确定选型 |
| AutoDL 接入 | `AutoDLClient` Provider Adapter | 独立 `provider/autodl` 模块 | 已落地，需模块化 |
| 工作流执行 | 常驻 ComfyUI GPU 节点池 | RabbitMQ + Celery Worker | 近期建设 |
| 工作流弹性 | SkyPilot Services/Warm Pool | Kubernetes + KEDA补充队列弹性 | SkyPilot优先 |
| 大模型 API 路由 | 暂不引入 | LiteLLM，仅承担模型路由 | MaaS 阶段引入 |
| LLM 推理 | 暂无 | vLLM/SGLang，按模型验证选择 | MaaS 阶段引入 |
| 图像/视频推理 | 表单化工作流产品 | ComfyUI Headless API | 产品方向已确定 |
| 对象存储 | 当前未形成统一层 | 阿里云 OSS/S3 兼容对象存储 | 工作流上线前必需 |
| 缓存与分布式锁 | 当前无 | Redis，仅作缓存、锁和短状态 | 按规模引入 |
| 边缘网关 | Nginx | 保持，不引入 Kong/Traefik | 已落地 |
| 服务进程 | systemd | 容器化后再迁移 Kubernetes | 已落地 |

核心原则：平台控制业务，供应商提供资源；MySQL记录业务事实，供应商返回资源事实；队列只负责传递任务，不能成为订单、余额和任务状态的事实源。

## 2. 全局技术架构树

```text
炫界云 AI 算力聚合平台
├── 1. 用户与产品入口层
│   ├── 算力租赁控制台
│   │   ├── GPU 地区/型号/库存
│   │   ├── 镜像与应用选择
│   │   ├── 实例创建、开机、关机、释放
│   │   └── 小时租/天租/周期计费
│   ├── 应用市场
│   │   ├── 预装环境应用
│   │   ├── 一键部署
│   │   └── 应用实例管理
│   ├── AI 工作流平台
│   │   ├── 工作流广场
│   │   ├── 文生图表单
│   │   ├── 文生视频表单
│   │   ├── 图生视频表单
│   │   ├── 运行记录与结果发布
│   │   └── V1 不开放节点画布
│   ├── 模型 API 平台（后续）
│   │   ├── API Key
│   │   ├── 模型路由
│   │   ├── Token/次数计量
│   │   └── OpenAI 兼容接口
│   └── 移动 SSH 工作台
│       ├── SSH Session
│       ├── 手机命令行
│       └── 远程文件浏览
│
├── 2. 边缘网关层
│   ├── DNS
│   │   ├── yaochuang.tech
│   │   ├── www.yaochuang.tech
│   │   └── cloud.yaochuang.tech
│   ├── Nginx TLS 终止
│   ├── HTTP -> HTTPS
│   ├── HSTS
│   ├── 路径路由
│   │   ├── /compute -> 主控制台 6111
│   │   ├── /api -> 主业务 API 6111
│   │   └── /ssh -> 移动 SSH 服务 6112
│   ├── 请求大小、超时和连接管理
│   └── 访问日志与 trace_id 透传（待补）
│
├── 3. 平台控制面
│   ├── IAM 用户中心
│   │   ├── 注册、登录、登出
│   │   ├── Token 鉴权
│   │   ├── 主账户/子账户
│   │   └── 管理员权限
│   ├── 商品中心
│   │   ├── GPU 资源商品
│   │   ├── 镜像商品
│   │   ├── 应用商品
│   │   ├── 工作流商品
│   │   └── 模型 API 商品
│   ├── 交易中心
│   │   ├── 钱包余额
│   │   ├── 充值订单
│   │   ├── 冻结/扣费/退款
│   │   ├── 账单流水
│   │   ├── 发票
│   │   └── 合同
│   ├── 定价中心
│   │   ├── 供应商成本
│   │   ├── 平台加价
│   │   ├── 小时/天/月价格
│   │   ├── 工作流按次价格
│   │   └── Token 价格（后续）
│   ├── 调度控制面
│   │   ├── 调度意图 ProvisionTask
│   │   ├── ProviderBinding
│   │   ├── PricePolicy
│   │   ├── 资源候选与过滤
│   │   ├── Provider 路由
│   │   ├── 下发、同步、停止
│   │   └── 状态机与恢复
│   └── 工作流控制面
│       ├── 模板与版本
│       ├── 参数 Schema
│       ├── workflow_api.json 映射
│       ├── 任务入队
│       ├── 运行状态
│       ├── 结果资产
│       └── 创作者发布与分成（后续）
│
├── 4. 调度与异步执行层
│   ├── SkyPilot 核心调度器
│   │   ├── API Server
│   │   ├── Clusters：开发机/实例
│   │   ├── Jobs：训练/批处理/工作流任务
│   │   ├── Services：ComfyUI/模型在线服务
│   │   ├── Docker Runtime Environment
│   │   └── 多资源池候选、队列与故障回退
│   ├── 工作流任务队列
│   │   ├── RabbitMQ Durable Queue
│   │   ├── Celery Worker
│   │   ├── ACK/超时可恢复
│   │   └── MySQL 幂等业务状态
│   ├── Warm Pool 路由
│   │   ├── 节点健康
│   │   ├── 模型/工作流能力标签
│   │   ├── GPU 忙闲状态
│   │   ├── 并发上限
│   │   └── 最小排队时间
│   └── 弹性层（后续）
│       ├── Kubernetes GPU Node Pool
│       ├── NVIDIA Device Plugin
│       ├── KEDA Queue Scaler
│       └── SkyPilot 统一外部与自有容量
│
├── 5. Provider 与执行面
│   ├── AutoDL Provider Adapter
│   │   ├── 余额
│   │   ├── 镜像
│   │   ├── GPU 库存
│   │   ├── Deployment
│   │   ├── Container
│   │   └── Pro Instance
│   ├── 第二供应商 Adapter（待接入）
│   ├── 自有 GPU 资源池（后续）
│   ├── ComfyUI Headless Worker
│   │   ├── Wan 系列
│   │   ├── LTX 系列
│   │   └── 其他成熟工作流
│   └── LLM Worker（后续）
│       ├── vLLM
│       └── SGLang
│
├── 6. 数据层
│   ├── MySQL：业务唯一事实源
│   ├── OSS：图片、视频、输入文件、结果文件
│   ├── RabbitMQ：任务传输，不保存业务真相
│   ├── Redis：缓存、锁、限流，不参与业务裁决
│   └── Provider API：资源状态权威源
│
└── 7. 运维与治理层
    ├── systemd 服务管理
    ├── Nginx 网关
    ├── 宝塔站点与证书
    ├── Let's Encrypt 自动续签
    ├── Git + 自动发布脚本
    ├── 结构化日志与 trace_id（待补）
    ├── 指标与告警（待补）
    └── 数据备份与灾难恢复（待补）
```

## 3. 当前部署与网关架构

### 3.1 生产环境

```text
Internet
└── DNS -> 47.103.49.82
    └── 宝塔 Nginx :80/:443
        ├── TLS 证书：yaochuang.tech + www + cloud
        ├── yaochuang.tech/
        │   └── 302 -> /compute
        ├── www.yaochuang.tech/
        │   └── 302 -> /compute
        ├── cloud.yaochuang.tech/
        │   └── 302 -> /compute/app/market
        ├── /compute、/compute/*
        │   └── 127.0.0.1:6111 -> power-compute.service
        ├── /api/*
        │   └── 127.0.0.1:6111 -> FastAPI
        └── /ssh/*
            └── 127.0.0.1:6112 -> power-mobile-ssh.service
```

生产服务：

| 服务 | 监听 | 管理方式 | 职责 |
|---|---:|---|---|
| Nginx | 80/443 | 宝塔/Nginx | TLS、域名、反向代理 |
| `power-compute.service` | 127.0.0.1:6111 | systemd | 主前端与业务 API |
| `power-mobile-ssh.service` | 127.0.0.1:6112 | systemd | 独立 H5 SSH 工作台 |
| MySQL | 远程 RDS | 连接池 | 账户、交易、商品、调度和工作流数据 |

### 3.2 当前 AutoDL 开发机入口

```text
SeetaCloud 8443 外部入口
└── 本机 Nginx :6008
    ├── /compute、/compute/* -> power :6111
    ├── /api/health -> power :6111
    ├── /api/workflows/* -> power :6111
    ├── /api/compute/* -> power :6111
    ├── /api/{user|account|wallet|apps|...} -> power :6111
    ├── /ssh/* -> mobile SSH :6112
    └── 其他路径 -> newswap :6009

保留服务
└── :6006 为原项目服务，禁止修改
```

### 3.3 网关选型

当前继续使用 Nginx，不引入 Kong、APISIX、Traefik 或服务网格。

原因：

1. 当前只有两个后端服务，Nginx 已覆盖域名、TLS、路径反代和静态入口。
2. 现阶段瓶颈在业务状态、调度可靠性和计费闭环，不在网关吞吐。
3. 新增 API Gateway 会增加配置中心、插件、监控和运维成本，不能产生当前业务价值。

达到以下条件之一时再评估专业 API Gateway：

- 对外 API Key 产品正式上线；
- 独立后端服务超过 5 个；
- 需要租户级限流、签名、配额和用量计量；
- 需要灰度路由、统一熔断和供应商级流量治理。

## 4. 软件模块架构

### 4.1 当前代码事实

```text
power/
├── a9_compute_admin/          React + TypeScript + Vite
├── api.py                     FastAPI 主应用、业务与调度逻辑
├── autodl_client.py           AutoDL 原生 API Adapter
├── busi/user/                 用户业务模块
├── mysqldbpoolnew.py          MySQL 连接池
├── user_token_auth.py         Token 认证
├── sql/                       业务表结构
├── scripts/                   初始化与运维脚本
├── mobile_ssh_console/        独立移动 SSH 服务
└── tools/                     发布、检查和证书脚本
```

### 4.2 目标业务模块树

`api.py` 最终只负责创建应用、安装 Middleware、挂载 Router 和托管前端。业务按职责进入各自目录：

```text
power/
├── api.py
├── busi/
│   ├── user/
│   │   ├── user_api.py
│   │   └── user_service.py
│   ├── account/
│   │   ├── account_api.py
│   │   └── account_service.py
│   ├── wallet/
│   │   ├── wallet_api.py
│   │   └── wallet_service.py
│   ├── product/
│   │   ├── product_api.py
│   │   └── product_service.py
│   ├── app/
│   │   ├── app_api.py
│   │   └── app_service.py
│   ├── instance/
│   │   ├── instance_api.py
│   │   └── instance_service.py
│   ├── provider/
│   │   ├── provider_api.py
│   │   ├── provider_service.py
│   │   └── autodl_provider.py
│   ├── provision/
│   │   ├── provision_api.py
│   │   ├── provision_service.py
│   │   └── provision_policy.py
│   ├── workflow/
│   │   ├── workflow_api.py
│   │   ├── workflow_service.py
│   │   ├── workflow_dispatcher.py
│   │   └── workflow_schema.py
│   ├── model/
│   │   ├── model_api.py
│   │   └── model_service.py
│   └── message/
│       ├── message_api.py
│       └── message_service.py
├── infrastructure/
│   ├── mysql.py
│   ├── object_storage.py
│   ├── rabbitmq.py
│   └── redis.py
└── workers/
    ├── workflow_worker.py
    └── provider_sync_worker.py
```

模块拆分遵守“一文件、一验证、一替换”：新文件中每个方法的参数、业务策略、SQL、外部调用顺序和出参完成一致性验证后，才切换 Router 并删除 `api.py` 中的旧实现。

## 5. 调度架构与选型

### 5.1 调度对象

平台存在三种不同调度，不能用一个概念混在一起：

| 调度类型 | 输入 | 输出 | 当前方案 |
|---|---|---|---|
| 实例调度 | GPU、地区、镜像、计费方式 | 一台实例或一个容器 | SkyPilot + AutoDL Provider Bridge |
| 工作流调度 | 模板、提示词、图片、规格 | 图片或视频结果 | SkyPilot Service/Warm Pool + 异步队列 |
| 模型 API 调度 | 模型、Token、SLA | 推理响应 | 后续 LiteLLM + vLLM/SGLang |

### 5.2 当前实例调度链路

```text
用户创建实例/运行工作流
    ↓
业务 Service 完成身份、余额、商品和参数校验
    ↓
写入本地业务意图 cd_provision_task
    ↓
读取 ProviderBinding + PricePolicy
    ↓
生成 SkyPilot Task/Service Spec
    ↓
SkyPilot 资源候选与调度
    ├── 自有/租赁 Kubernetes GPU 节点
    ├── 已有 SSH 机器
    └── AutoDL Provider Bridge 申请的新资源
    ↓
以 image_id: docker:<平台镜像> 启动运行环境
    ↓
保存 SkyPilot cluster/job/service ID 与供应商资源 ID
    ↓
同步 SkyPilot 和供应商权威状态
    ↓
推进本地任务/实例投影，开通入口并开始计费
```

现有 `cd_provision_task` 承担调度日志，核心状态包括：

```text
local
  └── queued
      ├── dry_run
      ├── submitted
      │   └── starting
      │       └── running
      └── failed

running
  ├── stopped
  └── failed
```

目标状态机必须补充：

- `client_oid`：调用方业务唯一标识；
- `provider_request_id`：供应商请求标识；
- `attempt_no`：执行尝试次数，但不允许因超时盲目重发；
- `next_action`：明确待执行动作；
- `last_provider_status`：供应商最后权威状态；
- `version`：乐观锁，防止并发状态覆盖；
- `started_at/stopped_at/released_at`：计费边界事实；
- 唯一约束：同一业务意图不能重复创建资源。

### 5.3 SkyPilot核心调度架构

SkyPilot从第一阶段进入架构，不等待第二供应商。平台不再继续扩大`api.py`中的自研Docker调度逻辑，而是建立独立的SkyPilot Gateway，把业务请求转换成SkyPilot的Cluster、Job或Service。

```text
炫界云 FastAPI 业务控制面
    ↓
Provision Service（业务意图、余额、商品、状态机）
    ↓
SkyPilot Gateway（Task/Service Spec、资源约束、状态映射）
    ↓
SkyPilot API Server
    ├── Kubernetes资源池
    │   └── image_id: docker:<炫界云镜像>
    ├── 已有GPU机器
    │   └── SkyPilot Cluster/Job
    └── AutoDL Provider Bridge
        ├── AutoDL API申请资源
        ├── 等待资源可连接
        ├── 注册/接入SkyPilot计算池
        └── SkyPilot运行Docker任务或服务
```

SkyPilot对象与炫界云业务对象映射：

| 炫界云业务 | SkyPilot对象 | 说明 |
|---|---|---|
| GPU开发实例 | Cluster | 长生命周期、可SSH、可重复提交任务 |
| 一次性训练/批处理 | Managed Job | 排队、执行、恢复和日志 |
| ComfyUI工作流Worker | Service或Job | 常驻服务走Service，单次批任务走Job |
| vLLM/SGLang模型API | Service | 多副本在线服务 |
| 应用市场一键部署 | Cluster + Docker Runtime | 用平台镜像启动预装环境 |

Docker调度规范：

1. 每个商品/工作流版本绑定不可变镜像摘要，不直接使用漂移的`latest`；
2. SkyPilot Spec明确GPU、CPU、内存、磁盘、地区、端口、环境变量和挂载；
3. 平台密钥通过SkyPilot secrets或内部密钥系统下发，不写入YAML和数据库明文字段；
4. Kubernetes上直接把Docker镜像作为Pod运行环境，不在Pod内再运行Docker；
5. 服务入口由SkyPilot Endpoint/Kubernetes Ingress产生，再登记到平台实例；
6. SkyPilot内部状态不替代炫界云订单、余额和业务状态机；
7. AutoDL Provider事实和SkyPilot执行事实都要关联`client_oid`和`provision_task_id`。

SkyPilot状态建议使用独立持久化数据库。开发阶段可使用其本地状态，生产API Server使用独立PostgreSQL；炫界云MySQL仍保存商业业务和映射关系，两者职责不合并。

官方能力依据：

- [SkyPilot Overview](https://docs.skypilot.co/en/latest/overview.html)：统一云、Kubernetes、Slurm和已有机器，提供Clusters、Jobs、Services。
- [SkyPilot YAML](https://docs.skypilot.co/en/stable/reference/yaml-spec.html)：定义资源、Docker镜像、环境、Secrets、挂载和运行命令。
- [SkyPilot on Kubernetes](https://docs.skypilot.ai/en/latest/reference/kubernetes/kubernetes-getting-started.html)：使用`image_id: docker:<image>`运行自有镜像。
- [SkyPilot Architecture](https://docs.skypilot.co/en/latest/reference/architecture/internals.html)：API Server状态和外部数据库边界。

### 5.4 第二阶段资源池选型

当平台掌握可安装Agent、可管理容器的GPU节点时，以Kubernetes作为SkyPilot可调度的底层资源池，KEDA只补充队列驱动的Pod扩缩容，不取代SkyPilot。

```text
平台业务控制面
└── SkyPilot API Server
    ├── 外部供应商容量 -> Provider Bridge
    └── 自有 GPU 容量 -> Kubernetes
        ├── GPU Node Pool
        ├── NVIDIA Device Plugin
        ├── ComfyUI Deployment
        ├── vLLM/SGLang Deployment
        └── KEDA 根据队列长度扩缩容
```

选择SkyPilot + Kubernetes + KEDA的原因：

- SkyPilot统一资源池、Job、Service和Docker任务定义；
- 工作流和模型推理最终都是容器化服务；
- KEDA可直接根据队列长度扩缩容；
- GPU、监控、存储和推理服务生态完整；
- 避免 Nomad 与 Kubernetes 两套调度、网络和运维体系并存。

当前不选择 Slurm、Volcano、Crossplane、Bacalhau：

- Slurm/Volcano偏自有集群批任务，不适合当前供应商 API 聚合；
- Crossplane偏基础设施声明式控制面，当前供应商没有现成 Provider；
- Bacalhau的去中心化计算模型与当前商业交付模型不一致。

## 6. 工作流执行架构

### 6.1 V1 产品边界

V1只做文生图、文生视频和图生视频的黑盒表单调用：

```text
工作流模板卡片
    ↓
详情页表单
    ├── 提示词
    ├── 参考图
    ├── 比例
    ├── 时长
    ├── 清晰度
    └── Seed
    ↓
运行并查看结果
```

V1不做 React Flow 节点画布，不允许用户修改公开节点图。成熟 ComfyUI 工作流由平台在后台维护，前端只暴露经过审核的业务参数。

### 6.2 执行链路

```text
POST /api/workflows/runs
    ↓
鉴权 + 模板状态 + 参数 Schema 校验
    ↓
计算价格并冻结算力币
    ↓
MySQL 创建 workflow_run（client_oid 唯一）
    ↓
将任务 ID 投递 RabbitMQ
    ↓
Celery Worker 获取任务
    ↓
Warm Pool Router 选择能力匹配且负载最低的节点
    ↓
填充平台维护的 workflow_api.json
    ↓
POST ComfyUI /prompt
    ↓
监听执行结果/进度
    ↓
输入和结果上传 OSS
    ↓
MySQL 提交 succeeded + 结果资产 + 实际成本
    ↓
确认扣费；失败则解冻/退款
    ↓
前端轮询或 SSE 获取结果
```

### 6.3 工作流调度策略

第一版按以下顺序选择 Worker：

1. 模型与自定义节点能力完全匹配；
2. 节点健康且最近心跳有效；
3. 当前运行数小于节点并发上限；
4. 预计排队时间最短；
5. 同等条件选择单位成本更低节点；
6. 没有可用节点时保持排队，不伪造成功，不临时冷启动新实例阻塞用户请求。

Warm Pool 节点至少上报：

- `worker_id`、地区、供应商；
- GPU 型号、数量、显存；
- 已安装模型和工作流版本；
- 当前任务、队列长度、显存占用；
- 心跳时间和健康状态；
- ComfyUI API 地址的内部标识，不能直接返回用户。

### 6.4 队列选型

工作流V1选择 RabbitMQ + Celery：

- RabbitMQ提供持久队列、ACK、消费确认和明确的投递语义；
- Celery与当前 Python/FastAPI 技术栈一致；
- 任务只传 `run_id`，完整参数和状态保存在 MySQL；
- Worker必须幂等，同一个 `run_id` 不能重复扣费或重复发布结果；
- Celery结果后端不作为业务事实源。

Redis后续用于缓存、限流、短锁和SSE进度，不用来裁决余额、订单和最终任务状态。

## 7. 模型 API 架构（后续）

统一账户和计费继续由炫界云平台负责，不直接采用第三方网关自带的用户与余额系统。

```text
客户 OpenAI 兼容请求
    ↓
Nginx/API Gateway
    ↓
炫界云 API Key 鉴权、额度和计量
    ↓
LiteLLM Router
    ├── 第三方模型 API
    ├── 自有 vLLM Endpoint
    └── 自有 SGLang Endpoint
    ↓
用量事实回写 MySQL
    ↓
账单流水
```

LiteLLM只负责模型协议统一、路由和供应商回退，不负责平台用户、充值和商业账本。图像/视频扩散工作流不交给vLLM，继续走ComfyUI工作流执行链路。

## 8. 数据架构

### 8.1 数据权威关系

| 数据 | 权威源 | 本地用途 |
|---|---|---|
| 用户、账户、余额、订单、账单 | MySQL | 业务裁决 |
| 商品、价格策略、供应商绑定 | MySQL | 调度输入 |
| 调度意图和本地状态 | MySQL | 幂等、恢复、审计 |
| 实例真实运行状态 | Provider API | MySQL保存投影和最后事实 |
| 工作流最终状态 | MySQL + Worker执行事实 | 前端查询、计费 |
| 图片、视频、输入文件 | OSS | MySQL保存对象Key和元数据 |
| 队列消息 | RabbitMQ | 仅传输任务ID |
| 缓存、锁、实时进度 | Redis | 可丢弃投影，不参与业务裁决 |

### 8.2 交易一致性

资源创建和工作流执行都必须遵循：

```text
创建业务唯一标识
    ↓
本地事务写入业务意图 + 冻结余额
    ↓
提交事务
    ↓
调用供应商/投递队列
    ↓
根据外部 ACK 幂等推进状态
    ↓
成功确认扣费；失败解冻
```

禁止：

- 请求超时后直接认定失败；
- 超时后自动重复创建资源；
- SQL失败后使用内存或JSON伪造成功；
- 队列成功投递等同于任务执行成功；
- 使用本地过期缓存覆盖供应商真实状态。

## 9. API 与异常标准

所有业务接口统一返回：

```json
{
  "err_code": 0,
  "err_desc": "",
  "data": {}
}
```

规则：

- API层只做参数定义、基础校验、身份依赖和Service调用；
- Service完成业务身份、业务校验、权威数据获取和状态提交；
- Provider Adapter只封装供应商原生协议，不承载平台价格和账户逻辑；
- SQL异常、网络异常、供应商拒绝、协议异常使用不同稳定错误码；
- `err_desc`使用可解释中文业务描述；
- SQL、堆栈、文件路径、局部变量、Token和供应商密钥不能返回客户端；
- 日志携带 `trace_id/user_id/biz_id/provider/action`；
- 外部请求和状态写入必须能够通过业务唯一标识关联。

## 10. 安全架构

```text
外部用户
├── HTTPS
├── 用户 Token/API Key
├── 业务权限与资源归属校验
└── 限流与审计

平台内部
├── Provider Token 仅存环境变量/密钥系统
├── 数据库凭证不写入代码
├── Worker 使用内部身份访问控制面
├── ComfyUI 不直接暴露公网
├── OSS 使用短期签名 URL
└── SSH 工作台独立服务、独立会话、独立审计
```

必须删除的高风险兼容逻辑：

- 无Token时默认使用用户1；
- 在代码文件中读取旧供应商Token；
- TLS证书失败后关闭校验；
- 用户明文密码字段继续写入；
- 对客户端返回SQL异常局部变量。

## 11. 可观测性

近期最小实现：

| 类型 | 指标/日志 |
|---|---|
| API | QPS、P95、P99、4xx/5xx、业务错误码 |
| 调度 | queued、submitted、running、failed、调度耗时 |
| Provider | 请求耗时、库存失败、限流、协议错误 |
| Workflow | 队列长度、等待时长、生成时长、成功率 |
| GPU Worker | 心跳、GPU利用率、显存、任务数、模型版本 |
| 交易 | 冻结金额、扣费、退款、对账差异 |
| 网关 | 域名、路径、状态码、上游耗时 |

告警优先级：

1. 余额重复扣减或账实不一致；
2. 同一业务标识创建多个供应商资源；
3. Provider接口持续失败；
4. 工作流队列持续增长且无可用Worker；
5. TLS证书、主服务或MySQL不可用；
6. 单个任务长时间停留在中间状态。

## 12. 分阶段实施路线

### Phase 1：稳定现有算力租赁控制面

- 按业务拆分 `api.py`；
- 统一用户身份和错误协议；
- 将Provider、Provision、Instance拆成明确模块；
- 部署SkyPilot API Server，建立平台SkyPilot Gateway；
- 先用一个平台Docker镜像跑通Cluster、Job、Service和状态同步；
- 将AutoDL资源ID、SkyPilot对象ID与`provision_task`建立映射；
- 为`cd_provision_task`补充幂等和状态恢复字段；
- 删除运行时建表、静默异常和JSON业务fallback；
- 完成余额冻结、扣费、退款和资源状态对账；
- 保持AutoDL为唯一已启用Provider。

### Phase 2：跑通第一个工作流闭环

- 选择一个成熟Wan/LTX ComfyUI工作流；
- 通过SkyPilot Service部署至少一个常驻4090 Warm Worker；
- 建立OSS输入/结果资产层；
- 引入RabbitMQ + Celery；
- 跑通表单、入队、执行、进度、结果、扣费、失败退款；
- 不做节点编辑画布。

### Phase 3：扩展SkyPilot多供应商调度

- 接入第二Provider Adapter；
- 建立统一资源规格、地区、库存和价格模型；
- 将第二Provider接入SkyPilot候选资源池；
- 实现成本、容量、SLA和地区约束下的候选排序；
- 建立供应商故障隔离和人工切流。

### Phase 4：自有GPU池与Serverless

- 建设Kubernetes GPU Node Pool；
- 部署ComfyUI/vLLM/SGLang Worker；
- 使用KEDA根据队列和流量扩缩；
- 形成外部供应商容量与自有容量的混合调度；
- 根据实际镜像分发瓶颈决定是否引入Dragonfly。

### Phase 5：模型API与生态

- 上线API Key、配额和用量计量；
- 引入LiteLLM模型路由；
- 对外提供OpenAI兼容接口；
- 工作流模板支持创作者发布、审核和分成；
- 建立企业账户、子账户、合同和成本中心。

## 13. 选型触发条件

技术组件只在业务条件满足时引入：

| 组件 | 引入条件 |
|---|---|
| RabbitMQ + Celery | 第一个真实异步工作流接入前 |
| OSS | 工作流开始接收用户图片和生成结果前 |
| Redis | 多进程锁、限流、SSE进度或热点缓存出现时 |
| SkyPilot | 立即进入核心调度建设，先跑自有Docker任务和AutoDL资源桥接 |
| Kubernetes | 平台真正拥有可管理GPU节点池时 |
| KEDA | Kubernetes上需要按队列自动扩缩时 |
| Dragonfly | 大镜像跨节点分发成为明确瓶颈时 |
| LiteLLM | 对外模型API和多模型路由上线时 |
| vLLM/SGLang | 自营LLM推理服务上线时 |
| 专业API Gateway | 外部API租户配额、签名和灰度能力超过Nginx职责时 |

## 14. 最终边界

炫界云长期需要自己掌握的核心能力：

- 统一用户与账户；
- 商品、定价、订单、余额和账单；
- Provider抽象和调度策略；
- 资源与工作流状态机；
- 工作流模板、参数Schema和结果资产；
- 多供应商成本与SLA治理；
- 用户入口、运营后台和数据分析。

可以使用成熟组件的能力：

- Nginx负责边缘入口；
- AutoDL等供应商负责底层资源；
- SkyPilot负责当前及未来的Docker任务、服务、资源候选与执行；
- RabbitMQ/Celery负责异步任务传递；
- ComfyUI负责图像/视频工作流执行；
- Kubernetes/KEDA负责未来自有GPU池弹性；
- LiteLLM和vLLM/SGLang负责未来模型API路由与推理。

平台不能把账户、账本、定价、业务状态机和工作流商品能力交给这些开源组件。它们服务于炫界云业务，而不是替代炫界云控制面。
