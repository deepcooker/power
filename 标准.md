# 执行层公开方法统一开发标准
> 外部系统泛指所有业务依赖的第三方远端服务，本标准适用于每一层、每一个对外公开方法。

## 方法执行流程（固定9步）
1. 入参定义
2. 入参基础校验
3. 业务身份与状态查询
4. 业务校验
5. 获取权威数据
6. 执行业务处理
7. 提交事实与状态变化
8. 构造标准出参
9. 异常归一化

## 具体要求
### 1. 入参定义
- 字段、类型、单位、来源、是否必填必须明确。
- 运行环境在ExecutionClient创建时固定，不在业务入参中重复传递。
- 不接收无法解释、没有业务使用者的参数。

### 2. 入参基础校验
- 仅校验类型、非空、数值范围、数据结构。
- 校验阶段不访问数据库、外部系统、配置中心。
- 基础校验层不实现业务策略门禁逻辑。

### 3. 业务身份与状态查询
- 根据业务身份，仅读取当前流程必需的业务事实。
- 实例入口：查询实例信息、账户绑定关系。
- 用户入口：查询用户、账户归属、API权限信息。
- 直接凭证入口：构造临时执行目标对象。
- 禁止多余查询、重复查询。

### 4. 业务校验
- 校验账户归属、业务状态、操作权限、运行环境、唯一写所有权。
- 校验外部系统能力、账户/资源类型适配性。
- 不编写无真实业务场景的防御保护条件。

### 5. 获取权威数据
- 账户、资源、订单、事件等业务真相，**以对应外部系统返回作为唯一真相源**。
- journal仅记录本地执行事实，不能替代外部系统返回结果。
- MySQL、Redis仅作为查询投影视图，不参与业务裁决逻辑。
- 禁止伪造默认值，禁止使用过期本地数据补齐真实业务事实。

### 6. 执行业务处理
- 单个公开方法只负责完成一个明确的业务动作。
- 原生API封装、ExecutionClient、路由层、应用服务层，各司其职，只处理本层职责。
- 所有技术治理逻辑，必须服务于请求下发、撤销、查询、状态恢复等具体业务场景。
- 禁止跨层直接访问外部API、密钥、数据库、策略内部状态。

### 7. 提交事实与状态变化
- 外部系统动作成功、产生可观测业务事实之后，才更新本地状态。
- 发起外部请求前，持久化完整业务意图与业务唯一标识（clientOid）。
- ACK、事件回执、撤销结果，严格按照外部系统返回事实做幂等状态推进。
- 禁止依靠超时直接判定业务失败，禁止因超时自动重复发起外部请求。

### 8. 构造标准出参
- 返回语义明确、结构不可变的标准业务对象。
- 返回对象的字段语义、计量单位、数据来源清晰可查。
- 禁止把外部系统原始内部字段直接透传给上层业务/策略。
- 不使用语义模糊的 success/data/message 简易拼装返回格式。

### 9. 异常归一化
- 已知业务错误，使用稳定错误码+中文业务描述。
- 网络异常、协议解析异常、外部系统拒绝、数据冲突，异常类型必须区分开。
- 异常信息携带业务身份、调用接口、关键业务字段；密钥、敏感凭证禁止出现在异常信息。
- 可恢复异常走标准化恢复流程；不可恢复异常直接明确失败，严禁静默吞掉异常。

## 总体原则
1. **业务驱动**：先明确业务动作，再选择对应的技术实现。
2. **语义对齐**：文件名、类名、方法名、注释，必须和实际业务职责保持一致。
3. **数据第一**：不丢失业务数据、不重复处理、不猜测状态、不伪造业务事实。
4. **效率极致**：热路径禁止数据库查询、禁止轮询、禁止重复解析；审计日志不阻塞主写流程。
5. **删除噪音**：没有调用方、无业务场景、逻辑重复、为兼容旧版本而存在的代码，一律不纳入代码库。
6. **不兼容旧版**：不保留历史别名、兼容分支、旧接口槽位、旧配置降级fallback、旧版入口。
7. **最小实现**：新增文件、方法、状态、防御门禁之前，必须确认有明确业务必要性。
8. **验证顺序**：单元测试 → 异常场景测试 → 性能基准测试 → 沙箱环境真实验收，完成后再评估生产接入。